- **环境要求**：Node.js >= 24 (由 `.nvmrc` 和 `package.json` engines 字段指定)。
- **启动命令**：`npm run dev` 启动开发服务器；`docker compose up app --build` 启动生产容器；`docker compose up dev --build` 启动开发容器。
- **AI 代理集成**：通过 `/clone-website` 技能触发多阶段逆向工程流程，支持 Claude Code、Codex CLI 等多种代理平台。